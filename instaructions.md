install the odin from the release and extract it
move all the contents of directory to somewhere like /opt/odin
ezport path to that dir for ease




sudo apt update
sudo apt install \
    libx11-dev \
    libxcursor-dev \
    libxrandr-dev \
    libxi-dev \
    libxinerama-dev \
    libgl1-mesa-dev



